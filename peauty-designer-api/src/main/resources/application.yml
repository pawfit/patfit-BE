server:
  port: 8080
  shutdown: graceful

spring:
  application:
    name: peauty-designer
  profiles:
    active: local
    include:
      - persistence
      - aws
  jackson:
    default-property-inclusion: non_null